return {
  {
    "MeanderingProgrammer/render-markdown.nvim",
    config = function()
      require("render-markdown").setup({
        -- Optional settings
        -- Configure as you like, or leave empty for defaults
      })
    end,
  },
}

